---

job:
  name: 'clusterverse/clusterverse-release-deploy'
  project-type: pipeline
  display-name: 'clusterverse-release-deploy'
  disabled: false
  pipeline-scm:
    scm:
      - git:
          url: https://github.com/dseeley/clusterverse.git
          branches:
            - master
          credentials-id: GITHUB_SVC_USER
          timeout: 20
    script-path: jenkinsfiles/Jenkinsfile_exec_release_deploy